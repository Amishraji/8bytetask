# Demo Node.js App

Minimal demo Node.js HTTP server.

Requirements
- Node.js (v14+ recommended)

Run locally
- Start with `node`:

```powershell
node index.js
```

- Or via npm script:

```powershell
npm start
```

Open your browser at http://localhost:3000
